@model School.Web.Models.Fees.DepositsViewModel
@{ ViewData["Title"] = "Deposits"; }

<div class="row g-3 mb-4">
    <div class="col-sm-6 col-xl-4">
        <div class="card border-0 shadow-sm h-100">
            <div class="card-body d-flex align-items-center gap-3">
                <div class="rounded-3 p-3 bg-success bg-opacity-10">
                    <i class="bi bi-safe text-success fs-3"></i>
                </div>
                <div>
                    <div class="text-muted small">Total Deposited</div>
                    <div class="fw-bold fs-4">₹@Model.TotalPaid.ToString("N2")</div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-sm-6 col-xl-4">
        <div class="card border-0 shadow-sm h-100">
            <div class="card-body d-flex align-items-center gap-3">
                <div class="rounded-3 p-3 bg-danger bg-opacity-10">
                    <i class="bi bi-arrow-counterclockwise text-danger fs-3"></i>
                </div>
                <div>
                    <div class="text-muted small">Total Refunded</div>
                    <div class="fw-bold fs-4">₹@Model.TotalRefunded.ToString("N2")</div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="card border-0 shadow-sm">
    <div class="card-header bg-white border-0 pt-3 d-flex justify-content-between align-items-center">
        <h6 class="mb-0 fw-semibold"><i class="bi bi-safe me-2 text-primary"></i>Deposits</h6>
        <a asp-action="Index" class="btn btn-sm btn-primary"><i class="bi bi-plus-circle me-1"></i>Record New Deposit</a>
    </div>
    <div class="card-body">
        @if (!Model.Items.Any())
        {
            <div class="text-center text-muted py-5">
                <i class="bi bi-safe" style="font-size:2.5rem;opacity:.3"></i>
                <p class="mt-2 mb-0">No deposit transactions yet</p>
            </div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-sm align-middle">
                    <thead>
                        <tr>
                            <th>Date</th>
                            <th>GR No.</th>
                            <th>Student</th>
                            <th>Deposit</th>
                            <th>Type</th>
                            <th class="text-end">Amount</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.Items)
                        {
                            <tr>
                                <td>@item.Date.ToString("dd MMM yyyy")</td>
                                <td>@item.GRNumber</td>
                                <td>@item.StudentName</td>
                                <td>@item.DepositName</td>
                                <td>
                                    @if (item.Type == "Paid")
                                    {
                                        <span class="badge bg-success-subtle text-success">Paid</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger-subtle text-danger">Refunded</span>
                                    }
                                </td>
                                <td class="text-end">₹@item.Amount.ToString("N2")</td>
                                <td class="text-end">
                                    <a asp-action="Collect" asp-route-studentId="@item.StudentId" class="btn btn-sm btn-outline-primary">
                                        <i class="bi bi-eye me-1"></i>View
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>
