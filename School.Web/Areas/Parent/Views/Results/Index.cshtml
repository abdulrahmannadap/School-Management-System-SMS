@model School.Web.Models.ParentPortal.ParentChildResultsViewModel
@{ ViewData["Title"] = "Exam Results"; }

<div class="card border-0 shadow-sm mb-3">
    <div class="card-body d-flex justify-content-between align-items-center">
        <h6 class="mb-0 fw-semibold"><i class="bi bi-person-circle me-2 text-primary"></i>@Model.Student.FullName</h6>
        <a asp-controller="Home" asp-action="Child" asp-route-studentId="@Model.Student.Id" class="btn btn-outline-secondary btn-sm">Back to Overview</a>
    </div>
</div>

<div class="card border-0 shadow-sm">
    <div class="card-header bg-white border-0 pt-3">
        <h6 class="mb-0 fw-semibold"><i class="bi bi-award me-2 text-primary"></i>Published Results</h6>
    </div>
    <div class="card-body">
        @if (!Model.Exams.Any())
        {
            <div class="text-center text-muted py-5">
                <i class="bi bi-award" style="font-size:2.5rem;opacity:.3"></i>
                <p class="mt-2 mb-0">No published exam results yet</p>
            </div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table align-middle">
                    <thead>
                        <tr>
                            <th>Exam</th>
                            <th>Period</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var exam in Model.Exams)
                        {
                            <tr>
                                <td>@exam.ExamName</td>
                                <td>@exam.StartDate.ToString("dd MMM yyyy") &ndash; @exam.EndDate.ToString("dd MMM yyyy")</td>
                                <td class="text-end">
                                    <a asp-action="Detail" asp-route-studentId="@Model.Student.Id" asp-route-examId="@exam.Id" class="btn btn-sm btn-outline-primary">
                                        <i class="bi bi-eye me-1"></i>View Result
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>
